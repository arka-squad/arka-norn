# Resume and Compare Audits

An interrupted audit resumes only when its confirmed workspace fingerprint still matches. Comparison classifies new, persistent, resolved and regressed findings, plus coverage changes. Audit IDs and evidence references remain stable across exports.
