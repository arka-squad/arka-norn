# ADR-001: Project Is the Canonical Repository Boundary

Status: accepted.

A Project marker defines the portable repository boundary and owns Features and Agent identity. Feature ownership is explicit through `projectId`; path proximity never substitutes for the relation.
