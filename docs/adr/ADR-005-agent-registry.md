# ADR-005: Agent Registry and Documentary Identity

Status: accepted.

Every signed document references an Agent identity verified against the owning Project registry and Feature scope. Sessions isolate current selections. Replacement deactivates the old identity while preserving lineage and historical documents.
